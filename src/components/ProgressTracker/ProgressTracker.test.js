import React from 'react';
import { cleanup, render, fireEvent } from 'react-testing-library';
import ProgressTracker from './ProgressTracker';

test('this is a small test, so jest does not complain', () => {
  expect(true).toBeTruthy();
});
